<UserControl x:Class="WeatherApp.Views.TemperatureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeatherApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Ville :"/>
            <TextBox Text="{Binding City, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
        </StackPanel>
        <Button DockPanel.Dock="Top" Content="Load content" Command="{Binding GetTempCommand}"/>
        <TextBox Text="{Binding RawText, Mode=OneWay}"/>
    </StackPanel>
</UserControl>
